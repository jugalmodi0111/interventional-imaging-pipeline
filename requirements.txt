# core
torch>=2.2
torchvision>=0.17
timm>=1.0                 # modality router: MobileNetV3 student (router.py imports it at load)
numpy
scipy
pandas
pyyaml
tqdm
opencv-python
scikit-image
scikit-learn
# medical / seg
monai>=1.3
nnunetv2>=2.5
SimpleITK
# detection
ultralytics>=8.2          # YOLOv8n / YOLO11n
pycocotools
# foundation / PEFT
segment-anything
peft>=0.11
transformers>=4.44        # Grounding DINO open-vocab auto-labeler (build-side only)
Pillow                    # PIL.Image, used by the GD labeler
# tabular (AVF surveillance / TAVR risk)
xgboost
lightgbm
shap
# audio (AVF bruit)
torchaudio
librosa
# export / edge
onnx
onnxscript                # required by torch>=2.9 onnx exporter
onnxruntime
onnxruntime-tools
openvino                  # Intel laptops; swap for tensorrt on Jetson/RTX
coremltools               # Apple silicon
# serving (Mac inference side; optional network API)
fastapi
uvicorn
python-multipart
# ingest (Dialygo Phase 1: institutional DICOM handover -> de-identified frame store)
pydicom>=3.0              # 3.x save_as(enforce_file_format=...) API is required by the fixture
pylibjpeg                 # plugin host: supplies the JPEG-Lossless / JPEG-LS decoders cath-lab cine needs
pylibjpeg-libjpeg         # JPEG Baseline/Extended/Lossless (1.2.840.10008.1.2.4.50/51/57/70)
pylibjpeg-rle             # RLE Lossless (1.2.840.10008.1.2.5)
openpyxl                  # vendor handovers ship the patient worksheet as .xlsx
